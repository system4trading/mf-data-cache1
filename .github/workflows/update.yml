name: Daily AMFI NAV update

permissions:
  contents: write   # 🔥 THIS LINE FIXES THE ERROR

on:
  schedule:
    - cron: '30 18 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - run: npm init -y
      - run: npm install node-fetch@3
      
      - run: npm install node-fetch

      - run: bash ./bashscript.sh

      - run: node scripts/fetch_amfi.js
      - run: node scripts/fetch_nifty.js
      - run: node scripts/build_category_avg.js

      - run: |
          git config user.name "mf-bot"
          git config user.email "bot@github.com"
          git add .
          git commit -m "Auto update MF & Nifty data" || true
          git push

